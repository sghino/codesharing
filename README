Example Code for http://codesharing.wordpress.com/author/sghinux/

Global setup:

 Set up git(https://github.com/account/ssh)
  git config --global user.name "Your Name"
  git config --global user.email javameglio@gmail.com
Add your public key(https://github.com/account/ssh)
      

Next steps:

  mkdir codesharing
  cd codesharing
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:sghinux/codesharing.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:sghinux/codesharing.git
  git push -u origin master
      

Importing a Subversion Repo?

  Check out the guide(http://help.github.com/import-from-subversion/) for step by step instructions.
      

When you're done:

  Continue

